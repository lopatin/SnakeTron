(function() {
  define(['underscore', 'backbone'], function(_, Backbone) {
    var GameModel;

    return GameModel = Backbone.Model.extend({
      initialize: function() {}
    });
  });

}).call(this);
